node_modules/
momni.db
momni.db-*
.env*
nohup.out
mobile/node_modules/
mobile/.env
.DS_Store

# uploaded digital products (live on the persistent disk in prod)
downloads/
# member-uploaded profile photos (live on the persistent disk in prod)
uploads/

# Expo local state and Supabase CLI temp files — never commit
mobile/.expo/
supabase/supabase/.temp/
# macOS folder icon files
Icon\r
